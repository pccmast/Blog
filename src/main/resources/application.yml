spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/blogdata?serverTimezone=UTC&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: lzp123456
  mvc:
    throw-exception-if-no-handler-found: true


server:
  port: 8808

mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
logging:
  level:
    blog.mapper: debug